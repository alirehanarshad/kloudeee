# kloudeee

Kloudeee is a browser-based cloud architecture + chatbot UI (Vite + React) with a lightweight Node HTTP API server.

## Run locally

```bash
npm install
npm run dev
```

## Environment variables

- `GROQ_API_KEY`
- `GEMINI_API_KEY`

## Deploy to Cloud Run

See `README.cloudrun.md`.

